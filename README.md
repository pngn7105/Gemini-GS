this is a fork of TMAPRICOT(Arshium)'s gemini google script client

the goals of this fork are to improve the UI, and upgrade the model to be more intelligent

new features include:
-improved UI sizing
-web search capability
-new model

NOTE: some of the contributions to the upstream of this fork were made by AI

the app is referred to as bard in itself to avoid suspicion(what google's ai used to be called)
